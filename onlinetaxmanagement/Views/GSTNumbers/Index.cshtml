@model IEnumerable<onlinetaxmanagement.Models.GSTNumber>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GSTNumber1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Registration.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GSTNumber1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Registration.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Gnid }) |
            @Html.ActionLink("Details", "Details", new { id=item.Gnid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Gnid })
        </td>
    </tr>
}

</table>
